/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Etiqueta} from './etiqueta';

describe('Etiqueta', () => {
  it('should create an instance', () => {
    expect(new Etiqueta()).toBeTruthy();
  });
});

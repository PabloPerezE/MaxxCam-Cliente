/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Inicio} from './inicio';

describe('Inicio', () => {
  it('should create an instance', () => {
    expect(new Inicio()).toBeTruthy();
  });
});

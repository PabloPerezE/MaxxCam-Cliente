/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { InicioEliminarComponent } from './inicio-eliminar.component';

describe('Component: InicioEliminar', () => {
  it('should create an instance', () => {
    let component = new InicioEliminarComponent();
    expect(component).toBeTruthy();
  });
});
